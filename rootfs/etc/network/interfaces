# Loopback only — Cheshire has no eth0 (Zynq-only)
auto lo
iface lo inet loopback

# TODO: Add wlan0 or other network interface when WiFi client auto-connect is needed
# auto eth0
# iface eth0 inet dhcp

