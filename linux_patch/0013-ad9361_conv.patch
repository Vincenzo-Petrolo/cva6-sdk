diff --git a/drivers/iio/adc/ad9361_conv.c b/drivers/iio/adc/ad9361_conv.c
index 1902e7d0..57ba1082 100644
--- a/drivers/iio/adc/ad9361_conv.c
+++ b/drivers/iio/adc/ad9361_conv.c
@@ -449,7 +449,15 @@ static int ad9361_dig_tune_delay(struct ad9361_rf_phy *phy,
 				 unsigned long max_freq,
 				 enum dig_tune_flags flags, bool tx)
 {
-	static const unsigned int rates[3] = {25000000U, 40000000U, 61440000U};
+	/*
+	 * Keep digital tune sweep hardcoded to 25/40/40 MHz on this tree.
+	 * 61.44 MHz is intentionally excluded due to known instability on
+	 * ZCU104-class LVDS integrations.
+	 */
+	// static const unsigned int rates[3] = {25000000U, 40000000U, 61440000U};
+	static const unsigned int rates[3] = {
+		25000000U, 40000000U, 40000000U
+	};
 	struct axiadc_converter *conv = spi_get_drvdata(phy->spi);
 	unsigned int s0, s1, c0, c1;
 	unsigned int i, j, r;
@@ -764,6 +772,11 @@ EXPORT_SYMBOL(ad9361_register_axi_converter);
 struct ad9361_rf_phy* ad9361_spi_to_phy(struct spi_device *spi)
 {
 	struct axiadc_converter *conv = spi_get_drvdata(spi);
+
+	if (!conv) {
+		return NULL;
+	}
+
 	return conv->phy;
 }
 EXPORT_SYMBOL(ad9361_spi_to_phy);
